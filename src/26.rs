fn main() {
    // Define variables
    let x = 10;
    let y = 5;

    // Perform calculations
    let result = x + y;

    // Print the result
    println!("The sum of {} and {} is {}", x, y, result);
}
